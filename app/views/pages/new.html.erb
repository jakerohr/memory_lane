<div class="container">
  <div col="row">
    <ul id="list1" class="pageSort">
        <% @partials.each do |p| %>
        <li><%= p.name %></li>
        <% end %>
    </div>
  </div>

  <ul id="list2" class="pageSort">
    <% @order.each_index do |e| %>
    <li><%= render "partials/#{@partials[e][:path]}" %></li>

    <% end %>

  </div>
</div>

